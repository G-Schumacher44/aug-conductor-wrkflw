# Conductor spine validator has zero pip dependencies — runs on stdlib only.
# python3 scripts/validate.py

# ── Optional LookML tooling ──────────────────────────────────────────────────
# Uncomment when approved for your environment. See demo/tools/ for eval briefs.

# lkml==1.3.7        — offline LookML syntax validation (demo/tools/lkml-validator.md)
# spectacles==2.4.17 — live LookML + SQL validation, requires Looker + BQ connection
