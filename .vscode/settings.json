{
  // ── File watcher (prevents CPU spike on rapid agent writes) ──────────────
  "files.watcherExclude": {
    "**/.git/**": true,
    "**/.venv/**": true,
    "**/project/**": true
  },
  "search.exclude": {
    "**/.venv": true,
    "**/project": true
  },
  "git.autorefresh": false,

  // ── LookML ───────────────────────────────────────────────────────────────
  "files.associations": {
    "*.lkml": "lookml"
  },
  "[lookml]": {
    "editor.tabSize": 2,
    "editor.insertSpaces": true,
    "editor.formatOnSave": false
  },

  // ── Python (validate.py) ─────────────────────────────────────────────────
  "python.defaultInterpreterPath": "python3",

  // ── Looker extension — fill in after provisioning a Looker connection ────
  // Do NOT hardcode credentials here. Set clientId/clientSecret as env vars
  // or via VS Code's secret storage (Cmd+Shift+P → "Preferences: Open User Settings").
  //
  // "looker.instanceUrl": "https://your-instance.looker.com",
  // "looker.clientId": "",
  // "looker.clientSecret": ""
}
